sudo: required
language: python
python:
  - "3.8"
os:
  - linux
install:
  - pip install -r requirements.txt
script:
  - python oakleaf.py -h
  - python oakleaf.py get hosts
  - python oakleaf.py get hosts -g jmc
  - python oakleaf.py get groups
  - python oakleaf.py get groups -g jmc
