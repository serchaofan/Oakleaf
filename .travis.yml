sudo: required
language: python
python:
  - "3.8"
os:
  - linux
install:
  - pip install -r requirements.txt
script:
  - sudo echo -e "[test]\nroot@118.24.16.115=woshigty123" > hosts
  - sudo cp oakleaf.py /usr/local/bin/oakleaf
  - export PATH=/usr/local/bin:$PATH
  - sudo oakleaf -h
  - sudo oakleaf get hosts
  - sudo oakleaf get hosts -g jmc
  - sudo oakleaf get groups
  - sudo oakleaf get groups -g jmc
  - sudo oakleaf run ping -a
  - sudo oakleaf run command -a echo hello
